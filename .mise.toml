[tools]
swiftlint = "0.63.2"
swiftformat = "0.61.1"

[tasks.fmt]
description = "Format Swift source files"
run = "swiftformat Sources Tests"

[tasks.lint]
description = "Lint Swift source files"
run = "swiftlint lint Sources Tests"

[tasks.test]
description = "Run tests"
run = "swift test"

[tasks.build]
description = "Build and bundle the app"
run = "sh build.sh"
